{
  "setup-worktree-windows": [
    "pip install -r requirements.txt",
    "echo %AGENT_ID% > .agent-id"
  ],
  "setup-worktree-unix": [
    "pip install -r requirements.txt",
    "echo $AGENT_ID > .agent-id"
  ]
}
